# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://evilmartians.github.io/lefthook/configuration/

pre-commit:
  parallel: true
  jobs:
    - run: npm run lint
      glob: "*.{js,ts,jsx,tsx}"

    - name: npm run test
      glob: "*"
commit-msg:
  scripts:
    "commitlint.sh":
      runner: bash